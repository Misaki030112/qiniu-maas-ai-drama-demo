export default function ProjectsIndexPage() {
  return (
    <section className="empty-project">
      <div className="empty-project__badge">项目工作台</div>
      <h1>先创建一个项目，再进入分阶段执行。</h1>
      <p>
        这里不再是“一把执行”的脚本面板。项目创建后，你会先在中间输入故事，再按阶段推进：剧本、角色、分镜、画面配音、输出合成，最后再接视频模型阶段。
      </p>
    </section>
  );
}
